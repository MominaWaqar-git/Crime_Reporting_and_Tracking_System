@{
    Layout = null;
}
@model IDictionary<string, object>
<!DOCTYPE html>
<html>
<head>
    <title>Case Details - CrimeVision</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
            :root {
                --blue: #2563eb;
                --purple: #9333ea;
                --cyan: #06b6d4;
            }

            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
                font-family: 'Poppins', sans-serif;
            }

            body {
                background: #050816;
                color: white;
                padding: 20px;
                min-height: 100vh;
                display: flex;
                align-items: flex-start; /* Upar se start ho raha hai */
                justify-content: center;
                padding-top: 50px;
            }

            .blob {
                position: fixed;
                border-radius: 50%;
                filter: blur(120px);
                opacity: .4;
                z-index: -1;
            }

            .b1 {
                width: 400px;
                height: 400px;
                background: var(--blue);
                top: -100px;
                left: -100px;
            }

            .b2 {
                width: 400px;
                height: 400px;
                background: var(--purple);
                bottom: -100px;
                right: -100px;
            }

            .details-box {
                background: rgba(255,255,255,0.03);
                backdrop-filter: blur(20px);
                padding: 30px; /* Padding kam ki taake height compact ho */
                border-radius: 20px;
                border: 1px solid rgba(255,255,255,0.08);
                max-width: 1000px; /* Width barha di */
                width: 100%;
            }

            .header-line {
                height: 3px;
                width: 100%;
                background: linear-gradient(90deg, var(--blue), var(--purple), var(--cyan));
                margin: 15px 0;
                border-radius: 2px;
            }

            .header-section {
                display: flex;
                justify-content: space-between;
                align-items: center;
            }

            .badge {
                background: linear-gradient(90deg, var(--blue), var(--purple));
                padding: 6px 16px;
                border-radius: 20px;
                font-weight: 600;
                font-size: 0.9rem;
            }

            .info-grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 15px;
                margin-top: 15px;
            }

            .item {
                background: rgba(255,255,255,0.03);
                padding: 12px; /* Padding kam ki */
                border-radius: 12px;
                border: 1px solid rgba(255,255,255,0.05);
            }

            .upload-box {
                margin-top: 15px; /* Margin kam kiya */
                padding: 15px;
                border: 1px solid rgba(255,255,255,0.1);
                border-radius: 15px;
                background: rgba(0,0,0,0.2);
            }

            .btn-action {
                background: var(--purple);
                border: none;
                color: white;
                padding: 10px;
                border-radius: 8px;
                cursor: pointer;
                width: 100%;
                margin-top: 10px;
                font-weight: 600;
            }
    </style>
</head>
<body>
    <div class="blob b1"></div>
    <div class="blob b2"></div>

    <div class="details-box">
        <div class="header-section">
            <h2>Crime Report #@Model["ID"]</h2>
            <span class="badge">@Model["Status"]</span>
        </div>
        <div class="header-line"></div>

        @if (TempData["Success"] != null)
        {
            <div style="color:#4ade80; margin-bottom:15px; text-align:center;">@TempData["Success"]</div>
        }

        <div class="info-grid">
            <div class="item"><strong>Crime Type:</strong><br>@Model["CrimeType"]</div>
            <div class="item"><strong>Incident Date:</strong><br>@Model["IncidentDate"]</div>
            <div class="item"><strong>Citizen:</strong><br>@Model["CitizenName"]</div>
            <div class="item"><strong>Contact:</strong><br>@Model["CitizenPhone"]</div>
        </div>

        <div class="item" style="margin-top:15px;"><strong>Location:</strong><br>@Model["Location"]</div>
        <div class="item" style="margin-top:15px;"><strong>Description:</strong><br><p style="color:#aaa; font-size: 0.95rem;">@Model["Description"]</p></div>

        <div class="upload-box">
            <strong>Attach Evidence:</strong>
            <form asp-action="UploadEvidence" method="post" enctype="multipart/form-data">
                <input type="hidden" name="complaintId" value="@Model["ID"]" />
                <select name="category" style="width:100%; padding:8px; margin:8px 0; background:#050816; color:white; border:1px solid #374151; border-radius:5px;">
                    <option value="Photo">Photo Evidence</option>
                    <option value="Document">Official Document</option>
                    <option value="Digital">Digital/CCTV</option>
                </select>
                <input type="file" name="evidenceFile" required style="color:#aaa; margin-bottom:10px; font-size: 0.9rem;" />
                <button type="submit" class="btn-action">Upload Evidence</button>
            </form>
        </div>

        <div class="upload-box" style="margin-top: 15px; border: 1px solid #374151;">
            <strong>Evidence History:</strong>
            @if (ViewBag.EvidenceFiles != null && ((List<string>)ViewBag.EvidenceFiles).Count > 0)
            {
                foreach (var file in (List<string>)ViewBag.EvidenceFiles)
                {
                    <div style="margin-top:8px; background:rgba(255,255,255,0.05); padding:8px; border-radius:8px; border-left:4px solid var(--cyan);">
                        📄 <span style="font-size: 12px; color: #aaa;">@file.Split('_').Last()</span>
                        <a href="~/uploads/@file" target="_blank" style="color:var(--cyan); float:right; text-decoration:none; font-weight:600;">View</a>
                    </div>
                }
            }
            else
            {
                <p style="color:#666; font-size: 12px; margin-top:10px;">No evidence uploaded yet.</p>
            }
        </div>

        <a href="@Url.Action("Dashboard", "OfficerPortal")" style="display:block; margin-top:20px; color:#60a5fa; text-align:center; text-decoration:none;">← Back to Dashboard</a>
    </div>
</body>
</html>